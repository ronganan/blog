<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rong.blog.mapper.AcTagMapper">
    <select id="findAll" resultType="AcTag">
        select name, value from ac_tag
    </select>
    <!--<insert id="save" parameterType="AcTag">-->
        <!--insert into ac_tag values(null, #{title}, #{content})-->
    <!--</insert>-->
    <!--<delete id="delete" parameterType="_int">-->
        <!--delete from ac_tag where id=#{id}-->
    <!--</delete>-->
    <!--<update id="update" parameterType="AcTag">-->
        <!--update ac_tag set title=#{title} where id=#{id}-->
    <!--</update>-->
    <!--<select id="get" parameterType="_int" resultType="AcTag">-->
        <!--select * from ac_tag where id=#{id}-->
    <!--</select>-->
</mapper>