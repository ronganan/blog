<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rong.blog.mapper.AcTypeMapper">
    <select id="findAll" resultType="AcType">
        select name, value from ac_type
    </select>
    <!--<insert id="save" parameterType="AcType">-->
        <!--insert into ac_type values(null, #{title}, #{content})-->
    <!--</insert>-->
    <!--<delete id="delete" parameterType="_int">-->
        <!--delete from ac_type where id=#{id}-->
    <!--</delete>-->
    <!--<update id="update" parameterType="AcType">-->
        <!--update ac_type set title=#{title} where id=#{id}-->
    <!--</update>-->
    <!--<select id="get" parameterType="_int" resultType="AcType">-->
        <!--select * from ac_type where id=#{id}-->
    <!--</select>-->
</mapper>